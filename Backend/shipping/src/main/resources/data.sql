-- ============================================================
-- SEED DATA: shipping-service (schema: shipping)
-- Sin seeds: las rutas y envíos se crean por API/eventos reales
-- (order.ready_for_shipping → shipment; admin crea rutas).
-- No-op para que Spring no falle con script vacío.
-- ============================================================
SELECT 1;
